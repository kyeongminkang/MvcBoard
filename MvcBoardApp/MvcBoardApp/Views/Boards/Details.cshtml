@*@model MvcBoardApp.ViewModels.BoardComment*@
@model MvcBoardApp.ViewModels.Page
@using Microsoft.AspNetCore.Identity
@inject SignInManager<IdentityUser> SignInManager
@inject UserManager<IdentityUser> UserManager
@{
    ViewData["Title"] = "GetBoardComment";
}
@{
    ViewData["Title"] = "Details";
}

<h1>Details</h1>

<div>
    <h4>Board</h4>
    <hr />

    <p></p>
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.board.UserName)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.board.UserName)
        </dd>
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.board.Subject)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.board.Subject)
        </dd>
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.board.Content)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.board.Content)
        </dd>
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.board.WriteDate)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.board.WriteDate)
        </dd>
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.board.CommentCount)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.board.CommentCount)
        </dd>
    </dl>
</div>
<div>
    <a asp-controller="Comments" asp-action="Create" asp-route-id="@Model.board.Id">Comment</a> |
    <a asp-controller="Boards" asp-action="Index" asp-route-pageNumber="@Model.PageIndex">Back to List</a>
</div>
<hr />

<table class="table">
    <thead>
        <tr>
            @*<th>
                @Html.DisplayNameFor(model => model.Comment.BoardId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Comment.Id)
            </th>*@
            <th>
                @Html.DisplayNameFor(model => model.comment.CommentUserName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.comment.CommentContent)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model.Comments)
        {
            <tr>
                @*<td>
                    @Html.DisplayFor(modelItem => item.BoardId)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Id)
                </td>*@
                <td>
                    @Html.DisplayFor(modelItem => item.CommentUserName)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.CommentContent)
                </td>
                <td>
                    <a asp-controller="Comments" asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                    <a asp-controller="Comments" asp-action="Details" asp-route-id="@item.Id">Details</a> |
                    <a asp-controller="Comments" asp-action="Delete" asp-route-id="@item.Id">Delete</a>
                </td>
            </tr>
        }
    </tbody>
</table>
